{
  "name": "vendasta-slidev",
  "version": "0.1.0",
  "scripts": {
    "dev": "slidev",
    "build": "slidev build",
    "export": "slidev export",
    "test": "node tests/index.test.js && node tests/server.test.js && node tests/generate-snapshot.test.js",
    "generate": "ts-node scripts/generateSlides.ts",
    "generate-snapshot": "ts-node scripts/generateSnapshotSlides.ts",
    "start": "go run server.go"
  },
  "dependencies": {
    "slidev-theme-vendasta": "file:theme/vendasta"
  },
  "devDependencies": {
    "@slidev/cli": "^51.8.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.2.2"
  }
}
